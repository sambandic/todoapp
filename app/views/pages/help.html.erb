<h1>To create an help page, first create the route in the routes file:

get '/help', to: 'pages#help'

Then define an help action in pages_controller.rb file:
def help
end

Create a new file called help.html.erb in the views/pages folder and fill it in:
<h1>This is the help page</h1>

>